---
- hosts: "{{ director_node_ssh_ip }}"
  connection: ssh
  roles:
    - etc-hosts
    - osp-director
    - nfs-shares
    - geerlingguy.nfs
    - osp-prep-for-overcloud
    - osp-overcloud
    - osp-test-data
    - add-test-gateway-ip
    - read-overcloudrc-props
    - rh-tempest
    - report_status

#- hosts: overcloud_controller
#  remote_user: root
#  roles:
#  - add-test-gateway-ip
#  - rh-tempest
