---

# TODO: not sure how this works out for multi-controller environments.
- name: hack in ip for tests
  shell: ip addr | grep -q {{ overcloud_bm_network_gateway }}/24 || ip a add {{ overcloud_bm_network_gateway }}/24 dev br-ex
